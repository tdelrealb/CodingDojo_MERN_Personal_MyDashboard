import { Layout } from '../../utils/Layout/Layout';
import styles from './Work.module.css';

export const Work = () => {
	return (
		<div>
			<Layout
				area='Work'
				headerDescription='“Setting goals is the first step in turning the invisible into the visible”.'
			/>
		</div>
	);
};
